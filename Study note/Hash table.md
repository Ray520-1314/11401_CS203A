讀書筆記：Hash Table（雜湊表）

基本概念

Hash Table（雜湊表） 是一種鍵值對（key–value）資料結構，透過 雜湊函數（hash function） 將 key 對應到一個陣列索引位置。
在雜湊函數設計良好、且負載因子受控的情況下，查詢、插入、刪除的期望時間複雜度皆為 O(1)。

由於不同的 key 可能對應到相同索引，碰撞（collision）無法避免，因此必須搭配碰撞處理策略，例如：
	•	Separate Chaining：每個索引位置維護一條串列
	•	Open Addressing：發生碰撞時改用探測方式尋找空位

在最壞情況（大量碰撞）下，操作可能退化為 O(n)，但實務上雜湊表仍是最快速且最常用的查詢結構之一。


從陣列與連結串列到雜湊表的演進思路

雜湊表可以視為兩個基本資料結構的自然延伸：
	1.	陣列
	•	已知索引時可 O(1) 存取
	•	但無法直接用「key」找到索引
	2.	連結串列
	•	插入與刪除方便
	•	但搜尋需要線性掃描 O(n)

雜湊表的目的，就是結合兩者的優點，同時避開缺點。


步驟一：只有陣列，索引是問題

陣列的優點是：
	•	依索引讀寫為 O(1)

但若資料是以 key 來存取（例如姓名、字串、ID）：
	•	並不知道該 key 對應哪個索引
	•	只能將 (key, value) 放進陣列後線性搜尋
→ 查詢時間仍是 O(n)


步驟二：使用「桶子陣列 + 串列」

為了避免每次都掃描整個陣列：
	•	建立一個陣列，每個位置稱為一個 bucket
	•	每個 bucket 存放一組 key 屬於該 bucket 的資料（通常用連結串列）

此時：
	•	查詢、插入、刪除只需在「某一個 bucket」內進行
	•	成本取決於該 bucket 的長度

這就是 Separate Chaining 的基本想法。


步驟三：加入雜湊函數來選 bucket

接著需要一個規則，將 key 對應到 bucket：
	•	雜湊函數將 key 映射成一個整數
	•	再將結果限制在陣列大小範圍內，得到 bucket 索引

整體流程為：
	1.	對 key 計算雜湊值
	2.	找到對應的 bucket
	3.	在該 bucket 內進行搜尋或更新


為什麼雜湊表「期望是 O(1)」

關鍵在於 負載因子（load factor）：
	•	設 n 為元素數量
	•	設 m 為 bucket 數量
	•	負載因子 α = n / m（平均每個 bucket 的元素數）

若：
	•	雜湊函數能讓 key 均勻分布
	•	並透過擴容與重新雜湊控制 α 不要過大

則：
	•	每個 bucket 平均很短
	•	查詢、插入、刪除的期望時間為 O(1 + α)
→ 在 α 有界時，視為 期望 O(1)
Hash Table ADT 支援的操作
	•	Insert / Put：插入新的 (key, value)，若 key 已存在則更新
	•	期望：O(1)，最壞：O(n)
	•	Lookup / Get：依 key 取得對應的 value
	•	期望：O(1)，最壞：O(n)
	•	Update：更新既有 key 的 value
	•	期望：O(1)，最壞：O(n)
	•	Delete / Remove：移除指定 key
	•	期望：O(1)，最壞：O(n)
	•	Contains：檢查 key 是否存在
	•	期望：O(1)，最壞：O(n)
	•	Traversal / Iteration：遍歷所有 (key, value)
	•	O(n)，順序通常不保證
	•	Resize / Rehash：調整表大小並重新雜湊
	•	O(n)（但可攤銷）


雜湊函數的設計（Hash Function Design）

雜湊函數的目標是將 key 映射成陣列索引：
	•	索引 = 雜湊值 對 表大小取餘數

一個好的雜湊函數應具備：
	•	決定性：相同 key 永遠產生相同結果
	•	均勻性：key 能平均分散到各個 bucket
	•	效率高：計算成本低
	•	低相關性：輸入微小變化，輸出差異要大


常見雜湊方式
	•	整數 key
	•	乘法雜湊（實務常見）
	•	取餘數雜湊（需謹慎選擇表大小）
	•	字串 key
	•	多項式 rolling hash（逐字累積，避免溢位）


表大小（m）的選擇
	•	Separate Chaining
	•	常選質數或規律成長的大小
	•	Open Addressing
	•	常用 2 的冪次方（效率高）
	•	雜湊函數需充分混合位元，避免低位偏差


碰撞處理策略（Collision Resolution）

設：
	•	n：元素數量
	•	m：bucket / slot 數量
	•	負載因子 α = n / m

